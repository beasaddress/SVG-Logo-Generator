//importing shapes classes
const Square = require("../lib/Square");
const Triangle = require("../lib/Triangle");
const Circle = require("../lib/Circle");

//function that will generate the logo based on shapre choice data
 function generateLogo(data) {
    //creating a variable for shape to be undefined before data is read
    let shape = undefined
    if(data.shape === "")
 }
